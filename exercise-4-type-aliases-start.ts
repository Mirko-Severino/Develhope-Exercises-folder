// Type alias definition
type Country = string;

// Variables with Country type
const country1: Country = "Nigeria";

const country2: Country = "Italy";

const country3: Country = "China";

// Export statement to avoid the error: "Cannot redeclare block-scoped variable 'name'."
export {};
